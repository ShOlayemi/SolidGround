"use client";

/** Sentry initialization seam; activate once SENTRY_DSN is provisioned. */
export function SentryInit() {
  // TODO: import * as Sentry from "@sentry/nextjs";
  // TODO: if (process.env.NEXT_PUBLIC_SENTRY_DSN) Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN });
  return null;
}
