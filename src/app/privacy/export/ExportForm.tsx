'use client';
import { useState } from 'react'; import { exportMyData } from './actions';
export function ExportForm(){const [status,setStatus]=useState('');const run=async()=>{setStatus('Preparing your export…');const r=await exportMyData();if(r.data){const url=URL.createObjectURL(new Blob([r.data],{type:'application/json'}));const a=document.createElement('a');a.href=url;a.download='solidground-data-export.json';a.click();URL.revokeObjectURL(url);setStatus('Your export is ready.')}else setStatus(r.error??'Export failed.')};return <div><button onClick={run} className="rounded-lg bg-slate-900 px-5 py-3 text-sm font-medium text-white hover:bg-indigo-700">Generate JSON export</button>{status&&<p className="mt-4 text-sm text-slate-600" aria-live="polite">{status}</p>}</div>}
