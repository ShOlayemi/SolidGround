'use client';
import { useState } from 'react'; import { deleteMyData } from './actions';
export function DeleteForm(){const [confirmed,setConfirmed]=useState(false);const [status,setStatus]=useState('');return <form action={async()=>{if(!confirmed)return;setStatus('Deleting…');const r=await deleteMyData();setStatus(r.success?'Your account and data have been deleted.':r.error??'Deletion failed.')}} className="rounded-2xl border border-red-200 bg-white p-6 shadow-sm"><label className="flex gap-3 text-sm text-slate-700"><input type="checkbox" checked={confirmed} onChange={e=>setConfirmed(e.target.checked)} className="mt-1 accent-red-600" />I understand this is irreversible and want to delete my data.</label><button disabled={!confirmed} className="mt-6 rounded-lg bg-red-600 px-5 py-3 text-sm font-medium text-white disabled:cursor-not-allowed disabled:opacity-40">Delete my data</button>{status&&<p className="mt-4 text-sm text-slate-600" aria-live="polite">{status}</p>}</form>}
